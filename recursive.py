file = open(stupidcanvas.txt)


def recursive(node1):
    file.write(node.ascii)
    if(right_node != nullpointer):
        recursive(right_node_pointer)
    #Drawn the first line

    if (left_node == nullpointer and down_node != nullpointer):
        file.write("\n")
        recursive(down_node_pointer)
    

    

node_object{
    up-pointer = some_node,
    left-pointer = some_node,
    down-pointer = some_node,
    right-pointer = some_node,
    value = ascii_char,
}

